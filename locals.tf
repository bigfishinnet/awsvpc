# locals {
#   common_name_prefix = "${format("%s-%s-%s", var.project,var.component,var.environment)}"

#   default_tags = {
#     Project     = "${var.project}"
#     Component   = "${var.component}"
#     Environment = "${var.environment}"
#   }
# }
